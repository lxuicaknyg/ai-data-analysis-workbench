# Production environment example
# Copy this file to .env.production and fill in values for your deployment.

# Base URL for the report agent backend, without trailing API paths.
# The frontend will call endpoints such as /api/optimize, /api/sql/test and /api/db/test through this base URL.
VITE_BANK_AGENT_BASE_URL=http://your-internal-service-ip:8001
